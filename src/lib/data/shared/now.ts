/**
 * What I'm working on now, shown in the hero. Before `start` it reads "from <date>".
 * Update this when an assignment changes; the weekly rebuild keeps the wording current.
 */
export const now = { client: 'Belastingdienst', via: 'Competa IT', start: '2026-10-01' };
